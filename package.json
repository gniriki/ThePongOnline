{
  "name": "the-pong-online",
  "version": "0.0.0",
  "description": "ThePongOnline",
  "main": "app.js",
  "author": {
    "name": "Oem"
  },
  "dependencies": {
    "express": "^4.14.0",
    "socket.io": "^1.7.2"
  },
  "devDependencies": {
    "browserify": "^14.0.0",
    "typescript": "^2.1.5"
  },

  "scripts": {
    "serve-http": "node server.js",
    "bundle-js": "browserify client/js/game.js -o client/js/bundle.js",
    "compile-ts": "tsc",
    "start": "npm run compile-ts && npm run bundle-js && npm run serve-http"
  }
}
